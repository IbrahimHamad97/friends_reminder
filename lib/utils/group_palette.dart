// Default group accent used when the user has not chosen a color yet.

/// Opaque ARGB value used as the initial [GroupRow.colorArgb] for new groups (teal).
///
/// Returns: 32-bit color with full alpha (`0xFF......`).
int defaultGroupColorArgb() => 0xFF00897B;
